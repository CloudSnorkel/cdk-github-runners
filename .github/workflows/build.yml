# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: build
on:
  pull_request: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      self_mutation_happened: ${{ steps.self_mutation.outputs.self_mutation_happened }}
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Install dependencies
        run: yarn install --check-files
      - name: build
        run: npx projen build
      - name: Find mutations
        id: self_mutation
        run: |-
          git add .
          git diff --staged --patch --exit-code > repo.patch || echo "self_mutation_happened=true" >> $GITHUB_OUTPUT
        shell: bash
        working-directory: ./
      - name: Upload patch
        if: steps.self_mutation.outputs.self_mutation_happened
        uses: actions/upload-artifact@v4.6.2
        with:
          name: repo.patch
          path: repo.patch
          overwrite: true
      - name: Fail build on mutation
        if: steps.self_mutation.outputs.self_mutation_happened
        run: |-
          echo "::error::Files were changed during build (see build log). If this was triggered from a fork, you will need to update your branch."
          cat repo.patch
          exit 1
      - name: Backup artifact permissions
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
      - name: Upload artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: build-artifact
          path: dist
          overwrite: true
  self-mutation:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: always() && needs.build.outputs.self_mutation_happened && !(github.event.pull_request.head.repo.full_name != github.repository)
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download patch
        uses: actions/download-artifact@v5
        with:
          name: repo.patch
          path: ${{ runner.temp }}
      - name: Apply patch
        run: '[ -s ${{ runner.temp }}/repo.patch ] && git apply ${{ runner.temp }}/repo.patch || echo "Empty patch. Skipping."'
      - name: Set git identity
        run: |-
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Push changes
        env:
          PULL_REQUEST_REF: ${{ github.event.pull_request.head.ref }}
        run: |-
          git add .
          git commit -s -m "chore: self mutation"
          git push origin "HEAD:$PULL_REQUEST_REF"
  package-js:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ !needs.build.outputs.self_mutation_happened }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          path: .repo
      - name: Install Dependencies
        run: cd .repo && yarn install --check-files --frozen-lockfile
      - name: Extract build artifact
        run: tar --strip-components=1 -xzvf dist/js/*.tgz -C .repo
      - name: Move build artifact out of the way
        run: mv dist dist.old
      - name: Create js artifact
        run: cd .repo && npx projen package:js
      - name: Collect js artifact
        run: mv .repo/dist dist
      - name: Save artifact
        uses: actions/upload-artifact@v6
        with:
          name: js-package
          path: dist
  package-java:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ !needs.build.outputs.self_mutation_happened }}
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          path: .repo
      - name: Install Dependencies
        run: cd .repo && yarn install --check-files --frozen-lockfile
      - name: Extract build artifact
        run: tar --strip-components=1 -xzvf dist/js/*.tgz -C .repo
      - name: Move build artifact out of the way
        run: mv dist dist.old
      - name: Create java artifact
        run: cd .repo && npx projen package:java
      - name: Collect java artifact
        run: mv .repo/dist dist
  package-python:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ !needs.build.outputs.self_mutation_happened }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          path: .repo
      - name: Install Dependencies
        run: cd .repo && yarn install --check-files --frozen-lockfile
      - name: Extract build artifact
        run: tar --strip-components=1 -xzvf dist/js/*.tgz -C .repo
      - name: Move build artifact out of the way
        run: mv dist dist.old
      - name: Create python artifact
        run: cd .repo && npx projen package:python
      - name: Collect python artifact
        run: mv .repo/dist dist
      - name: Save artifact
        uses: actions/upload-artifact@v6
        with:
          name: python-package
          path: dist
  package-dotnet:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ !needs.build.outputs.self_mutation_happened }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          path: .repo
      - name: Install Dependencies
        run: cd .repo && yarn install --check-files --frozen-lockfile
      - name: Extract build artifact
        run: tar --strip-components=1 -xzvf dist/js/*.tgz -C .repo
      - name: Move build artifact out of the way
        run: mv dist dist.old
      - name: Create dotnet artifact
        run: cd .repo && npx projen package:dotnet
      - name: Collect dotnet artifact
        run: mv .repo/dist dist
  package-go:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ !needs.build.outputs.self_mutation_happened }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-go@v6
        with:
          go-version: ^1.18.0
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          path: .repo
      - name: Install Dependencies
        run: cd .repo && yarn install --check-files --frozen-lockfile
      - name: Extract build artifact
        run: tar --strip-components=1 -xzvf dist/js/*.tgz -C .repo
      - name: Move build artifact out of the way
        run: mv dist dist.old
      - name: Create go artifact
        run: cd .repo && npx projen package:go
      - name: Collect go artifact
        run: mv .repo/dist dist
  validate-python-advanced:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/advanced
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/advanced
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/advanced
  validate-python-composite-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/composite-provider
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/composite-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/composite-provider
  validate-python-ec2-windows-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/ec2-windows-provider
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/ec2-windows-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/ec2-windows-provider
  validate-python-ecs-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/ecs-provider
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/ecs-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/ecs-provider
  validate-python-ecs-windows-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/ecs-windows-provider
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/ecs-windows-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/ecs-windows-provider
  validate-python-multi-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/multi-provider
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/multi-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/multi-provider
  validate-python-provider-selector:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/provider-selector
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/provider-selector
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/provider-selector
  validate-python-simple-codebuild:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: python-package
          path: dist
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: examples/python/simple-codebuild
      - name: Install local package
        run: ls ../.. && pip install ../../dist/python/*.tgz
        working-directory: examples/python/simple-codebuild
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/python/simple-codebuild
  validate-typescript-advanced:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/advanced
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/advanced
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/advanced
  validate-typescript-composite-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/composite-provider
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/composite-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/composite-provider
  validate-typescript-ec2-windows-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/ec2-windows-provider
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/ec2-windows-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/ec2-windows-provider
  validate-typescript-ecs-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/ecs-provider
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/ecs-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/ecs-provider
  validate-typescript-ecs-windows-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/ecs-windows-provider
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/ecs-windows-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/ecs-windows-provider
  validate-typescript-multi-provider:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/multi-provider
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/multi-provider
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/multi-provider
  validate-typescript-provider-selector:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/provider-selector
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/provider-selector
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/provider-selector
  validate-typescript-simple-codebuild:
    needs:
      - package-js
      - package-python
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
      - name: Download build artifacts
        uses: actions/download-artifact@v6
        with:
          name: js-package
          path: dist
      - name: Install dependencies
        run: npm install
        working-directory: examples/typescript/simple-codebuild
      - name: Debug
        run: find .
      - name: Install local package
        run: ls ../.. && npm install ../../dist/js/*.tgz
        working-directory: examples/typescript/simple-codebuild
      - name: CDK Synth
        run: cdk synth
        working-directory: examples/typescript/simple-codebuild
